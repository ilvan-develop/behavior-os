---
description: Run behaviorOS demo mission
---

Execute `pnpm demo` — loads `behavior-os/missions/demo.json` + `behavior-os/workflows/development.json`, validates governance, runs workflow stages, writes `behavior-os/runtime/demo.json` with COMPLETED. Requires prior `pnpm install` and `pnpm typecheck`.
