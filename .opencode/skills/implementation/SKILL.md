---
name: implementation
description: Use when implementing changes, editing files or applying patches. Triggers on implement, code, fix.
---

# Implementation

Implement strictly per plan/architecture. Keep edits minimal, preserve `package.json` and `src/` of host, write tests in `tests/`.
