---
name: architecture
description: Use when designing architecture, boundaries or contracts. Triggers on architect, architecture, adr.
---

# Architecture

Define interfaces, module boundaries, and integration contracts. Write ADRs in `docs/adr/`. Respect `src/domain` vs `src/core` vs `adapters` separation.
