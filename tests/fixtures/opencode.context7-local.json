{
  "$comment": "Fixture overlay (fix-mcp-docs-cleanup): espelha opencode.json mcp.context7 local {env:} enabled true. Nunca token literal.",
  "mcp": {
    "context7": {
      "type": "local",
      "command": ["npx", "-y", "@upstash/context7-mcp", "--api-key", "{env:CONTEXT7_API_KEY}"],
      "enabled": true
    }
  }
}
