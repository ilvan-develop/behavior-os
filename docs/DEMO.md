# Demo — behaviorOS

```bash
pnpm install
pnpm demo
```

Saída:

```
[demo] behaviorOS demo — Mission → Workflow → Evidence
[demo] COMPLETED — mission=demo workflow=development
[demo] evidence: behaviorOS/runtime/demo.json
```

Verifique:

```bash
cat behaviorOS/runtime/demo.json
pnpm doctor
```
